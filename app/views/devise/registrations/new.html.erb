<%= render 'home/sidebar' %>

<%=image_tag("top-image.png", class: "top-page-image") %>

<div class="content-container">  
  
  <div class="content-info">
    <%= link_to (raw('Uitloggen')), destroy_user_session_path, :method => :delete, class:"pull-right" %>
    <h4>< Ga terug</h4>
    <h2>Je bent hier:</h2>
    <h1>Instellingen</h1>
    <h3>Nieuwe Zorggever toevoegen</h3>
  </div>

  <div class="content-signup">
    <div class="col-sm-9 signup-form">
      <h2>Nieuwe zorggever</h2>
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= devise_error_messages! %>

        <div class="field">
          <input type="text" placeholder="Voor- en achternaam">
        </div>

        <div class="field">
          <input type="text" placeholder="Gebruikersnaam">
        </div>

        <div class="field">
          <%= f.email_field :email, autofocus: true, placeholder:"E-mailadres" %>
        </div>

        <div class="field">
          <%= f.password_field :password, autocomplete: "off", placeholder:"Wachtwoord (6 karakters minimaal)" %>
        </div>

        <div class="field">
          <%= f.password_field :password_confirmation, autocomplete: "off", placeholder:"Wachtwoord bevestigen" %>
        </div>

        <div class="field">
          <input type="file" name="file" id="file" class="inputfile" />
          <label for="file">Upload een profiel foto</label>
        </div>

        <div class="actions pull-right">
          <%= f.submit "Opslaan" %>
        </div>
      <% end %>
    </div>
  </div>

</div>
